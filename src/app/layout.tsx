import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
  preload: true,
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
  preload: false, // Only load when needed
});

export const metadata: Metadata = {
  title: "AI Revolution - Future of Artificial Intelligence",
  description: "Experience the future of artificial intelligence with cutting-edge technology and innovative solutions",
  keywords: "AI, artificial intelligence, machine learning, technology",
  authors: [{ name: "AI Revolution Team" }],
  openGraph: {
    title: "AI Revolution",
    description: "Experience the future of artificial intelligence",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
